export enum GateType {
  AND = 'AND',
  OR = 'OR',
  XOR = 'XOR',
  NOT = 'NOT',
  NAND = 'NAND',
  NOR = 'NOR',
}

export enum CipherType {
  CAESAR = 'Caesar',
  XOR = 'XOR',
}

export enum CircuitType {
  ADDER = '4-Bit Adder',
  SUBTRACTOR = '4-Bit Subtractor',
  COUNTER = '4-Bit Counter',
}
